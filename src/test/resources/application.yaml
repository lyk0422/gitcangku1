spring:
  datasource:
    # 测试专用命名内存库（MySQL 兼容模式）；连接池关闭后数据库随之释放。
    url: jdbc:h2:mem:test_demo;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jackson:
    time-zone: UTC
