spring:
  datasource:
    # 测试使用独立命名的 H2 内存库（MySQL 兼容模式），随测试上下文共享连接池
    url: jdbc:h2:mem:airspace_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
