spring:
  application:
    name: gitcangku1-test
  datasource:
    url: jdbc:h2:mem:water-test;MODE=MySQL;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
    hikari:
      maximum-pool-size: 10
  sql:
    init:
      mode: always
  jackson:
    time-zone: Asia/Shanghai

server:
  port: 0
