spring:
  datasource:
    # 测试专用命名内存库，与本地运行库隔离；JVM 退出即释放。
    url: jdbc:h2:mem:exposure_cap_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  jackson:
    time-zone: UTC
