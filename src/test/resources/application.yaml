spring:
  datasource:
    url: jdbc:h2:mem:firmware_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    username: sa
  sql:
    init:
      mode: always
      encoding: UTF-8
