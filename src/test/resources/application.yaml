# 测试配置：使用独立命名的 H2 内存库（MySQL 兼容模式），不依赖任何外部数据库。
spring:
  datasource:
    url: jdbc:h2:mem:blindexp_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  jackson:
    time-zone: Asia/Shanghai

logging:
  level:
    root: warn
