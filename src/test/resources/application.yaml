spring:
  application:
    name: gitcangku1-test
  datasource:
    url: jdbc:h2:mem:calibration;MODE=MySQL;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
    hikari:
      maximum-pool-size: 10
  sql:
    init:
      mode: always

server:
  error:
    include-message: never
    include-stacktrace: never
