spring:
  application:
    name: gitcangku1-test
  datasource:
    url: jdbc:h2:mem:curtailment;MODE=MySQL;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always

server:
  port: 0
